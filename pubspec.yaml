name: generative_ui_webgpu
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0-254.0.dev

dependencies:
  flutter:
    sdk: flutter
  onnxruntime: ^1.4.1
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/vocab.txt
    - assets/models/bge-small-en-v1.5/onnx/model.onnx
